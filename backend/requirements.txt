# Pinned for reproducible installs (CI uses Python 3.12).
# Bump deliberately after smoke / unit tests pass — do not leave unpinned.
# Transitive deps (pydantic, starlette, PyMuPDF, …) resolve from these pins.

fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
pdf2docx==0.5.13
pypdf==6.17.0
Pillow==12.3.0
zhconv==1.4.3
pypdfium2==5.13.0
